This program requires some modules, 
you can download them by printing this commands in your console:
pip install Flask
pip install requests
pip install beautifulsoup4

Then just download the folder and run this commands:
cd C:\Users\Lenovo\Desktop\weather-scrapper
python app.py

After that, open http://127.0.0.1:5000/ in your browser